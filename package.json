{
  "name": "pmo",
  "private": "true",
  "scripts": {
    "frontend:dev": "npm -prefix frontend run start",
    "backend:dev:mvn": "cd backend && mvnw spring-boot:run"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}